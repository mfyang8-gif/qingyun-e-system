<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>com.yangmf</groupId>
        <artifactId>qingyun-e-system</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>qingyun-admin</artifactId>
    <name>qingyun-admin</name>
    <description>Web 应用入口 - Controller 层 + Spring Boot 启动类</description>

    <dependencies>
        <!-- ========== 内部模块 (framework 传递 common, system/ai 传递 common) ========== -->
        <dependency>
            <groupId>com.yangmf</groupId>
            <artifactId>qingyun-framework</artifactId>
        </dependency>
        <dependency>
            <groupId>com.yangmf</groupId>
            <artifactId>qingyun-system</artifactId>
        </dependency>
        <dependency>
            <groupId>com.yangmf</groupId>
            <artifactId>qingyun-ai</artifactId>
        </dependency>

        <!-- ========== Test ========== -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.amqp</groupId>
            <artifactId>spring-rabbit-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.springframework.security</groupId>
            <artifactId>spring-security-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
        </plugins>
    </build>
</project>
